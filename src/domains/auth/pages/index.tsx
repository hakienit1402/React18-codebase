/**
 * Auth pages barrel.
 *
 * Purpose:
 * - Centralize exports for route-level pages within the Auth domain.
 * - Enable concise imports: `import { LoginPage } from '@/domains/auth/pages';`
 *
 * How to add a new page:
 * 1. Create the page component file in this folder, e.g. `LoginPage.tsx`.
 * 2. Re-export it here:
 *    `export { LoginPage } from './LoginPage';`
 */

// Keep this as a module even when empty.
export {};
